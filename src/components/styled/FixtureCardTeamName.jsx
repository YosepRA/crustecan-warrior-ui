import Typography from '@mui/material/Typography';
import { styled } from '@mui/material/styles';

const FixtureCardTeamName = styled(Typography)(() => ({
  fontSize: '1.2rem',
  textTransform: 'uppercase',
}));

export default FixtureCardTeamName;
